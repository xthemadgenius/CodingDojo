# 1 countdown
def countdown(num):
    new = reversed(list(range(0, num)))
    return new
print(countdown(5))
# 2 print and return
# 3 first plus length
# values greater than Second
# this length, that value